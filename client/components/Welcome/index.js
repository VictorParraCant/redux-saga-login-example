import React, {Component, PropTypes as PT} from "react";

const Welcome = ({}) => {
    return (
        <div>
            Welcome
        </div>
    );
};

Welcome.propTypes = {
};

Welcome.defaultProps = {
};

export default Welcome;
